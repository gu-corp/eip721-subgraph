{
  "name": "@gu-corp/eip721-subgraph",
  "version": "1.0.1-build.0",
  "description": "EIP-721 Subgraph",
  "scripts": {
    "clean": "rimraf build generated",
    "auth": "graph auth",
    "remove": "graph remove",
    "create": "graph create",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "graph": "graph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.96.0",
    "@graphprotocol/graph-ts": "^0.38.1"
  }
}
