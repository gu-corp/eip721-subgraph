name: Deploy subgraph
run-name: Deploy eip721 subgraph ${{ github.ref_name }} for ${{ inputs.network }} by @${{ github.actor }}
on:
  workflow_dispatch:
    inputs:
      network:
        description: 'Network to deploy to'
        required: true
        type: choice
        options:
          - joc
          - joc-testnet
          - mainnet
          - sepolia
          - polygon

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.network }}
  cancel-in-progress: true

jobs:
  deploy-joc:
    name: Deploy to JOC
    if: ${{ inputs.network == 'joc' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @gu-corp/eip721-subgraph clean
      - run: pnpm --filter @gu-corp/eip721-subgraph codegen
      - run: pnpm --filter @gu-corp/eip721-subgraph build -- --network joc
      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./packages/subgraph/package.json').version")" >> "$GITHUB_OUTPUT"
      - run: pnpm --filter @gu-corp/eip721-subgraph graph auth ${{ secrets.THE_GRAPH_EIP_721_JOC_DEPLOY_KEY }}
      - run: pnpm --filter @gu-corp/eip721-subgraph graph deploy eip-721-joc --node https://api.studio.thegraph.com/deploy/ --version-label ${{ steps.version.outputs.value }}

  deploy-joc-testnet:
    name: Deploy to JOC Testnet
    if: ${{ inputs.network == 'joc-testnet' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @gu-corp/eip721-subgraph clean
      - run: pnpm --filter @gu-corp/eip721-subgraph codegen
      - run: pnpm --filter @gu-corp/eip721-subgraph build -- --network joc-testnet
      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./packages/subgraph/package.json').version")" >> "$GITHUB_OUTPUT"
      - run: pnpm --filter @gu-corp/eip721-subgraph graph auth ${{ secrets.THE_GRAPH_EIP_721_JOC_TESTNET_DEPLOY_KEY }}
      - run: pnpm --filter @gu-corp/eip721-subgraph graph deploy eip-721-joc-testnet --node https://api.studio.thegraph.com/deploy/ --version-label ${{ steps.version.outputs.value }}

  deploy-mainnet:
    name: Deploy to Mainnet
    if: ${{ inputs.network == 'mainnet' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @gu-corp/eip721-subgraph clean
      - run: pnpm --filter @gu-corp/eip721-subgraph codegen
      - run: pnpm --filter @gu-corp/eip721-subgraph build -- --network mainnet
      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./packages/subgraph/package.json').version")" >> "$GITHUB_OUTPUT"
      - run: pnpm --filter @gu-corp/eip721-subgraph graph auth ${{ secrets.THE_GRAPH_EIP_721_MAINNET_DEPLOY_KEY }}
      - run: pnpm --filter @gu-corp/eip721-subgraph graph deploy eip-721-mainnet --node https://api.studio.thegraph.com/deploy/ --version-label ${{ steps.version.outputs.value }}

  deploy-sepolia:
    name: Deploy to Sepolia
    if: ${{ inputs.network == 'sepolia' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @gu-corp/eip721-subgraph clean
      - run: pnpm --filter @gu-corp/eip721-subgraph codegen
      - run: pnpm --filter @gu-corp/eip721-subgraph build -- --network sepolia
      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./packages/subgraph/package.json').version")" >> "$GITHUB_OUTPUT"
      - run: pnpm --filter @gu-corp/eip721-subgraph graph auth ${{ secrets.THE_GRAPH_EIP_721_SEPOLIA_DEPLOY_KEY }}
      - run: pnpm --filter @gu-corp/eip721-subgraph graph deploy eip-721-sepolia --node https://api.studio.thegraph.com/deploy/ --version-label ${{ steps.version.outputs.value }}

  deploy-polygon:
    name: Deploy to Polygon
    if: ${{ inputs.network == 'polygon' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @gu-corp/eip721-subgraph clean
      - run: pnpm --filter @gu-corp/eip721-subgraph codegen
      - run: pnpm --filter @gu-corp/eip721-subgraph build -- --network matic
      - name: Get version
        id: version
        run: echo "value=$(node -p "require('./packages/subgraph/package.json').version")" >> "$GITHUB_OUTPUT"
      - run: pnpm --filter @gu-corp/eip721-subgraph graph auth ${{ secrets.THE_GRAPH_EIP_721_POLYGON_DEPLOY_KEY }}
      - run: pnpm --filter @gu-corp/eip721-subgraph graph deploy eip-721-polygon --node https://api.studio.thegraph.com/deploy/ --version-label ${{ steps.version.outputs.value }}
